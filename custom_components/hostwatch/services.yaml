get_apt_summary:
  fields:
    device_id:
      selector:
        device:
          integration: hostwatch
          multiple: true
    output_options:
      collapsed: false
      fields:
        create_notification:
          example: true
          default: true
          selector:
            boolean:
        include_raw:
          example: false
          default: false
          selector:
            boolean:
get_bootloader_summary:
  fields:
    device_id:
      selector:
        device:
          integration: hostwatch
          multiple: true
    output_options:
      collapsed: false
      fields:
        create_notification:
          example: true
          default: true
          selector:
            boolean:
        include_raw:
          example: false
          default: false
          selector:
            boolean:
refresh_agent_updates:
